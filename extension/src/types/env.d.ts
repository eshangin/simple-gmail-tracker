// Type declaration for the __ENV__ global injected by webpack DefinePlugin.
// Values come from env/env.dev.json (dev build) or env/env.build.json (production build).
declare const __ENV__: {
    TRACKER_BASE_URL: string;
};
